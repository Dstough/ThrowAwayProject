@model HomeViewModel;
@if (Model.ErrorMessage != null)
{
    <script>
        $(function() {
            DisplayMessage('@Model.ErrorMessage');
        });
    </script>
    Model.ErrorMessage = null;
}

<div class="thread">
    <h1>Latest News Broadcasts</h1>
    [<a>view all</a>]
</div>

<div class="thread">
    <h1>Fixer Job Postings</h1>
    [<a>view all</a>]
</div>

<div class="thread">
    <h1>Run Transcript Archive</h1>
    [<a>view all</a>]
</div>

<div class="thread">
    <h1>Latest User Threads</h1>
    [<a>view all</a>]
    @foreach (var thread in Model.UserThreads)
    {
        <div class="post" onclick="location.href='@thread.Url';">
            <span class="message-body">
                @thread.Title
            </span>
            <div class="message-signature @thread.CSS ">
                -- @thread.Author <span class=time-stamp>@DateTime.Now.AddYears(60)</span>
            </div>
        </div>
    }
</div>