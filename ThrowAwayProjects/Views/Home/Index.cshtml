@model HomeViewModel;
@if (Model.ErrorMessage != null)
{
    <script>
    $(function() {
        DisplayMessage('@Model.ErrorMessage');
    });
    </script>
    Model.ErrorMessage = null;
}
@if (Model.NewsArticles.Count > 0)
{<h2>News Stories</h2>
    <br /><a href="#">view all</a>
    <div style="margin-bottom: 30px">
        @foreach (var article in Model.NewsArticles)
        {<div class="grid-card">
                <div class="img">
                    <img src="images/example.png" alt="" />
                </div>
                <a href="#">@article</a>
            </div>}
    </div>}
@if (Model.UserThreads.Count > 0)
{<h2>User Threads</h2>
    <br /><a href='/Forum/List/1'>view all</a>
    <div style="margin-bottom: 30px">
        @foreach (var thread in Model.UserThreads)
        {<article>
                <div class="thread-metadata text-muted">
                    <span style="@thread.Style">@thread.Author</span> -
                    <span>@thread.PostDate.AddYears(60).ToString("MMMM dd, yyyy hh:mm tt")</span>
                </div>
                <div style="text-align:justify;">
                    <h3 class="message-head" onclick=""><a href="@thread.Url">@thread.Title</a></h3>
                    <span class="message-body">@thread.Body</span>
                </div>
            </article>}
    </div>}
@if (Model.JobArticles.Count > 0)
{<h2>Job Listings</h2>
    <br /><a href="#">view all</a>
    <div style="margin-bottom: 30px">
        @foreach (var article in Model.JobArticles)
        {<div class="grid-card">
                <div class="img">
                    <img src="images/example.png" alt="" />
                </div>
                <a href="#">@article</a>
            </div>}
    </div>}
@if (Model.RunArticles.Count > 0)
{<h2>Run Archive</h2>
    <br /><a href="#">view all</a>
    <div style="margin-bottom: 30px">
        @foreach (var article in Model.RunArticles)
        {<div class="grid-card">
                <div class="img">
                    <img src="images/example.png" alt="" />
                </div>
                <a href="#">@article</a>
            </div>}
    </div>}