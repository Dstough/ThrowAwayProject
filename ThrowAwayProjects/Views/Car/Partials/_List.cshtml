@model List<CarViewModel>;
<table class="table table-hover">
    <thead>
        <tr>
            @if(Model.Count > 0)
            {
                <th>Make</th>
                <th>Model</th>
            }
            else
            {
                <th>No Records To Display</th>
            }
            <th> 
                <button id="AddNew" class="btn btn-warning btn-xs" onclick='Modal("@Url.Action("AddEdit","Car")");'>Add New</button>
            </th>
           
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.Make</td>
                <td>@item.Type</td>
                <td id="@item.Id" colspan='2' onclick='Modal("@Url.Action("AddEdit","Car",new{id=item.Id})");'><a class="edit"><span class="glyphicon glyphicon-pencil"></span></a></td>
            </tr>
        }
    </tbody>
</table>