@model TrainViewModel;
@using(Html.BeginForm("AddEdit","Train",FormMethod.Post, new { id = "FormEditTrain" }))
{
    <div class="modal-header bg-dark">
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
            <span aria-hidden="true">&times;</span>
        </button>
        <h2 class="modal-title">Train Information</h2>
        @Html.HiddenFor(m=>m.Id)
    </div>

    <div class='modal-body forms-inline'>
        
        <div class="input-group">
            <div class="input-group-addon">
                <div class="input-group-text">Name:</div>
            </div>
            @Html.TextBoxFor(m => m.Name, new{@class="form-control"})
        </div>

        <div class="input-group">
            <div class="input-group-addon">
                <div class="input-group-text">Cargo:</div>
            </div>
            @Html.TextBoxFor(m => m.Cargo, new{@class="form-control"})
        </div>

        <div class="input-group">
            <div class="input-group-addon">
                <div class="input-group-text">Cars:</div>
            </div>
            @Html.TextBoxFor(m => m.Cars, new{@class="form-control", type="number"})
        </div>

    </div>

    <div class='modal-footer'>
        @if(Model.Id == 0)
        {
            <input type="submit" id="btnSubmit" class="btn btn-primary" name="action" value="Add New" />
        }
        else
        {
            <input type="submit" id="btnSubmit" class="btn btn-primary" name="action" value="Edit Train" />
            <input type="submit" id="btnDelete" class="btn btn-danger pull-left" name="action" value="Delete">
        }
    </div>
    <script>
        $("#btnDelete").on("click", function(){
            return confirm("Are you sure you want to delete this Train?");
        });
    </script>
}